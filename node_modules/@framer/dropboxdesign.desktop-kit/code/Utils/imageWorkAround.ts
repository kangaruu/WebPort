import { url } from "framer/resource"

// FIXME: Workaround for loading canvas components that have bitmap images in them
export const urlWithoutDomain = ({ filename, filepath }) =>
    url(`${filepath}${filename}`).replace("/preview", "")
